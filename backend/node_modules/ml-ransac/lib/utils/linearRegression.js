"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.linearRegression = exports.line = void 0;
const ml_levenberg_marquardt_1 = require("ml-levenberg-marquardt");
/**
 * Get the line function with given slope and offset.
 *
 * @param root0 - Line parameters.
 * @param root0."0" - Slope.
 * @param root0."1" - Offset.
 * @returns The line function.
 */
function line([a, b]) {
    return (x) => a * x + b;
}
exports.line = line;
/**
 * Compute linear regression parameters for the given data.
 *
 * @param source - Source data.
 * @param destination - Destination data.
 * @param options
 * @returns Line parameters.
 */
function linearRegression(source, destination, options = {}) {
    const { initialValues = [0, 0] } = options;
    const result = (0, ml_levenberg_marquardt_1.levenbergMarquardt)({ x: source, y: destination }, line, {
        initialValues,
    });
    return result.parameterValues;
}
exports.linearRegression = linearRegression;
//# sourceMappingURL=linearRegression.js.map