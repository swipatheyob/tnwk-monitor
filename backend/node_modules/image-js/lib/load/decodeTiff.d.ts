import { decode } from 'tiff';
import { Image } from '../Image.js';
type TiffIfd = ReturnType<typeof decode>[number];
/**
 * Decode a TIFF. See the tiff module.
 * @param buffer - The data to decode.
 * @returns The decoded image.
 */
export declare function decodeTiff(buffer: Uint8Array): Image;
/**
 * Create image from a single IFD.
 * @param ifd - The IFD.
 * @returns The decoded image.
 */
export declare function getImageFromIFD(ifd: TiffIfd): Image;
export {};
//# sourceMappingURL=decodeTiff.d.ts.map